# Lab Tooling

Required baseline: AWS CLI, Terraform, Git, jq, curl and a configured AWS CLI profile.

Recommended: tfsec/Trivy, Checkov, terraform-docs and pre-commit.

Never commit credentials, tfstate, tfvars containing secrets, or generated cloud credentials.