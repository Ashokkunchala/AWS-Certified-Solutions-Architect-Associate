# Scenario Question Bank

Build questions around architectural decisions rather than memorized definitions.

Each question should record: requirement, constraints, candidate services, correct reasoning, distractor analysis and domain.