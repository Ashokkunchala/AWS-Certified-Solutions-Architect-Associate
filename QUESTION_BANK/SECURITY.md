# Security Scenarios

## Q1
An application needs a database that is never publicly reachable. Which network placement and access controls should be used?

**Reasoning target:** private subnet, restrictive security groups and controlled application-to-database traffic.

## Q2
A team needs temporary AWS permissions for a workload without storing long-lived keys. What identity pattern should be evaluated?

**Reasoning target:** IAM role and temporary credentials.