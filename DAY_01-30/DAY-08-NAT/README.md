# Day 8 — NAT and Private Subnets

## Lab
Place a test instance in a private subnet.

Verify:
1. no direct inbound internet path
2. outbound access through NAT when configured
3. route-table dependency

Record NAT cost considerations and cases where a VPC endpoint can be preferable.