# Day 26 — Performance and Caching

Given a slow workload, classify the bottleneck as compute, network, database, storage or repeated reads.

Evaluate caching, horizontal scaling, read scaling and CDN options.