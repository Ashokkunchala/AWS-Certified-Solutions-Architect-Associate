# Lab 05 — Private AWS Service Access

Create an S3 gateway endpoint and compare the traffic path with NAT-based access.

Study gateway endpoints for S3/DynamoDB, interface endpoints, endpoint policies, security groups and NAT gateway cost/availability trade-offs.

Evidence: route tables, endpoint state and a written comparison. Destroy after testing.