# Lab 06 — API Gateway + Lambda + DynamoDB

Build a minimal CRUD API. Add structured logs, error handling and least-privilege IAM.

Failure drill: deny one dependency permission and diagnose from logs.